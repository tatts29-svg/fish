## v6.50 — 26 Sep 2026 — the Broadcast has its takes

- Andrew: "Then broadcast activate and get that sorted." The programme's 35 turns had no takes (0 of 35), so Broadcast ran the scenes in silence. The takes were generated on his ElevenLabs account through the connector in this session: flow 2zKC9W0fRUvP06zjlYwD, eleven_v3, LEAD on his designed voice Gc500, COLOUR on Josh — Warm Conversational Australian, one take a turn, about 7,900 credits in all. Downloaded, measured, and put through pipeline/broadcast.py (mono, peak-normalised, 96 kbps) with the spine's per-slot lengths set to what each take measured, since the check exists to catch a take in the wrong slot and these were made in slot order.
- The hosted page carries them the way it carries every other piece of media: 35 files named by SHA-256 in the kit's media folder and the manifest (171 assets, digest ae4de01a…), `{"media": sha}` markers in DATA.broadcast, the manifest digest recomputed. Nothing else in the page changed: the Broadcast button, the player, the scene rule and the "holds" figures were already there.
- `test_v650.js` on the local service: 35 of 35 turns carry audio, the button offers "The commentary — two voices over the scenes", a press starts the programme on slot 1 from `/m/<token>/<sha>.mp3` (200, audio/mpeg), the element plays, a second press stops it; no script errors. Headless Chromium has no audio output so the clock barely advances there; the file decodes cleanly with ffmpeg (12.1 s, 44.1 kHz mono).
- `takes/` holds the 35 encoded takes by slot, role and voice; `programme.json` is the spine with the measured lengths.
