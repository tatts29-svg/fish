## v6.51 — 26 Sep 2026 — the V8 from three generated loops, and a crowd

- Andrew: "Improve the v8 sound." Three V8 loops (idle, mid revs, high revs) and a grandstand crowd bed were generated on his ElevenLabs account through the connector (eleven_text_to_sound_v2, flows 320nEUwxJJ0G85eJlBqA, 1GUwG3kMm1ksxM8AHiZQ, qHpMyxq0kEonkBkETLJC, psL9D2b7AbA23SWxcvJ3; two takes of each engine loop, the pick by analysis — the cleanest harmonic series — all seven takes kept under `sound/`). Treated by the sound pipeline (mono, peak −1 dBFS, 96 kbps, tail crossfaded over head; a short loop now gets a quarter-length crossfade rather than none) and carried by the hosted page as media (175 assets, digest 7ab31a2a…).
- In the scene (`gc3d_bundle.js`, sound part): a new engine mode, `loops` — the three loops each through its own gain into the same lowpass the designed engine uses, each pitched from its own note by the revs the gearbox reports, crossfaded by rpm over a trapezoid each (equal power). The intake noise, tyre scrub, upshift cut, downshift blip, overrun pops, distance, pan and Doppler ride on them unchanged. The crowd bed has its own gain straight to the master: up on the wide, drone and overhead shots, down beside the car, gone while the words take the scene. `loops` becomes the default the moment the three have decoded; the designed V8 stays one pick away.
- In the showcase: an Engine chooser beside the Sound button (Generated loops / Designed V8), shown while sound is on, kept on the device (`gc500.showengine`). Sound stays off until its button, as always.
- `test_v651.js`: sound on → mode loops, the three loops running with finite rates and gains (idle at 1.06× on the grid), the crowd playing, the chooser switching to the designed V8 and back; no script errors. Headless Chromium cannot judge it by ear — that is Andrew's call, and the designed V8 is there if he prefers it.
