## v6.53 — 26 Sep 2026 — choose the vehicle

- Andrew: "Add option to change vehicle" (and from 25 Sep: forklift, boom, scissor lift, tractor, each with a racing style, sliding round corners). A Vehicle chooser beside View in the showcase: the Coates #26, or Coates plant in the orange — a forklift, a boom lift, a scissor lift, a tractor. Kept on the device (`gc500.showvehicle`).
- In the scene (`gc3d_bundle.js`): `G.plantModel(kind, quality)` builds each machine from the same primitives the car is built from (boxes, tubes, patched cylinders, a wheel with a hub that turns on its axle and spins up in the burnout), in the same part format, so `drawRaceCar` draws them and the quality steps apply. Four new plain materials (Coates orange, black, white, steel) because the car's paint kind carries the car's own livery rules. A helmeted operator on each. `G.setVehicle(kind)` rebuilds the parts on the next draw and scales the lap speed and the cornering grip to the machine (forklift 48 % of the car's top speed, boom 38 %, scissor 34 %, tractor 55 %) — the same simulation, the same slide through the corners — and drops the engine note for the diesel plant (the loops and the designed V8 both).
- The car's decals are the car's: the plant carries none. Nothing about the record is touched; the credit line still says the backdrop is decoration.
- `test_v653.js`: each vehicle builds (19–26 parts, 1,200–1,700 triangles at Balanced), drives, the chooser is remembered, the car comes back with its speed restored; no script errors. `shots/` has each machine on the circuit by day.
