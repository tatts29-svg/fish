## v6.60 — 26 Sep 2026 — the Broadcast as a live race call

- Andrew: "the broadcast the voices need to sound like race car commentary they need to talk like its a race on right now. Supercar v8s live commentary. proceed and approved."
- The 35 turns keep their slots, sections, names, facts and "holds" figures; the words are called as live Supercars commentary — present tense, flat out, over the engine (`bc_live_script.json`). Re-voiced on his ElevenLabs account through the connector: flow houl7zfZXdRtQEN9kWNL, eleven_v3, Gc500 for LEAD, Josh for COLOUR, one take a turn. Turn 19's first attempt was declined at the permission prompt, so it was reworded without the "race engineer" line and generated once.
- Each take has a broadcast EQ (high-pass 90 Hz, +3 dB presence, compression) and a circuit bed under it — three layered crowd loops offset in time and pitch, shaped pink noise, and his original V8 clip swelling past every 17 seconds — about 17 dB under the voice, quieter under the closing line. The two ambience takes generated for this came back one second long, so the bed is built from what the page already had.
- `pipeline/broadcast.py` now loudness-normalises (loudnorm, −16 LUFS) instead of dynaudnorm, which pumped the bed up in every pause. The page carries the 35 new takes as media by SHA-256 and drops the 35 old ones from the manifest and its media table (175 assets, digest f44a0b2a…).
- Test: 35 of 35 turns carry audio; Broadcast plays slot 1 (15.0 s) from the hosted media and stops cleanly; no script errors. `takes/` holds the mixed takes; `SAMPLE_turn01_live_call.mp3` is the opener.
